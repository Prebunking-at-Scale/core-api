apiVersion: v1
kind: Service
metadata:
  name: core-api
  labels:
    app: core-api
spec:
  type: NodePort
  selector:
    app: core-api
    tier: web
  ports:
    - port: 8000
      targetPort: 8000
